pluginManagement {
    repositories {
        maven {
            url "https://artifacts.itemis.cloud/repository/gradle-plugins/"
        }
        gradlePluginPortal()
    }
}

rootProject.name = 'build.publish.mps'
